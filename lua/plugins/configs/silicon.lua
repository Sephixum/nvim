local silicon = require("silicon")

silicon.setup({
	theme = "auto",
	font = "JetBrains Mono",
	-- bgColor = colors.color2,
	-- bgColor = "#6e6e6e",
	bgColor = vim.g.terminal_color_6,
	roundCorner = true,
})
